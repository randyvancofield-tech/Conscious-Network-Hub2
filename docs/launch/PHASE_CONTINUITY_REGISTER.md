# Phase Continuity Register

Date captured: 2026-05-27

This register prevents launch-readiness work from becoming an informal “later” list. Every item has a status, owner, and next action.

| Item | Related Phase | Current Status | Reason | Owner | Next Action | Files or Systems Involved | Date Captured |
|---|---|---|---|---|---|---|---|
| Rich course authoring | Provider CRM completion | completed | Provider/admin course editing now supports short description, full description/body, tier, status, category/topic, estimated duration, learning objectives, content sections, owner association, and publish/archive state using existing `Course.syllabus` JSON. | Codex | Include in CEO launch walkthrough. | `server/src/services/courseMetadata.ts`, `server/src/services/providerCrmWorkspaceStore.ts`, `server/src/routes/courses.ts`, `server/src/routes/userCourses.ts`, `components/ProviderCrmShell.tsx`, `components/KnowledgePathways.tsx` | 2026-05-27 |
| Durable CRM tool visibility | Provider CRM completion | completed | Sole-admin workspace tool visibility is persisted in Postgres and enforced by backend route gates for Notes, Content/Courses, Collaboration, Follow-Ups, and Analytics. Local Neon reconcile/apply and launch data verification passed. | Codex | Include Provider CRM tool toggles in CEO launch walkthrough. | `server/src/services/providerCrmToolSettingsStore.ts`, `server/src/routes/providerCrm.ts`, `server/scripts/reconcile-launch-schema.js`, `server/scripts/verify-launch-data-paths.js` | 2026-05-27 |
| Course schema promotion from JSON to first-class columns | Database/data-model hardening | requires CTO decision | Launch requirements are satisfied through existing `Course.syllabus` JSON with no destructive migration. Promoting rich fields to columns is only needed if CTO wants SQL-level reporting/filtering beyond current launch behavior. | CTO | Decide whether first-class course metadata columns are required; if yes, approve a non-destructive migration plan. | `server/prisma/schema.prisma`, `Course.syllabus`, course/provider CRM routes | 2026-05-27 |
| Cloudflare dashboard verification | Deployment hardening | requires external dashboard action | Repo cannot verify Cloudflare account settings. Application security remains enforced in app code; dashboard checks are still required for routing, cache, and edge security. | Cloudflare admin / CEO / CTO | Confirm TLS Full Strict, HTTPS redirect, HSTS posture, Pages/static SPA fallback to `index.html`, canonical redirects preserve path/query, WAF/bot rules do not block app/API flows, cache bypass for HTML/API, Stripe return URLs preserve query strings, and Stripe webhooks target Render directly. | Cloudflare dashboard, `conscious-network.org`, Render backend URL, Stripe webhook endpoint | 2026-05-27 |
| Frontend chunk-size warning | Deployment hardening | requires dependency upgrade/testing | `App.tsx` eagerly imports major route surfaces and Vite has no manual chunking/lazy route split configured. Changing this during the CRM patch could affect broad navigation and needs visual/regression testing. | Codex / CTO | Add route-level `React.lazy`/`Suspense` or Vite manual chunking in a dedicated frontend performance pass, then run full build and route smoke tests. | `App.tsx`, `vite.config.ts`, major `components/*` route surfaces | 2026-05-27 |
| Legacy password migration retirement | Auth/security hardening | requires CTO/security decision | `verifyPassword` still supports scrypt, SHA-256, and plaintext legacy compatibility. Removing fallback could lock out accounts that have not rehashed through sign-in. | CTO / Security | Run a redacted DB count of non-bcrypt password records, decide migration/reset window, then remove fallback after affected accounts are rehashed or reset. | `server/src/auth.ts`, `server/src/routes/user.ts`, `User.password` records | 2026-05-27 |
| Multer 1.x upgrade | Upload/security hardening | requires dependency upgrade/testing | Upload routes use Multer memory storage for provider applicant documents and user media. Upgrading to Multer 2.x may change runtime/types and must be regression-tested against private upload authorization. | Codex / CTO | Branch upgrade to maintained Multer target, update types/imports if needed, run applicant upload, reflection upload, public media, private object-key denial, and build/test suite. | `server/package.json`, `server/package-lock.json`, `server/src/routes/upload.ts`, `server/src/routes/providerApplicants.ts`, `server/src/services/uploadBlobStore.ts` | 2026-05-27 |
| AI indexing consent granularity | AI/privacy hardening | requires CEO decision and CTO/security decision | Current crawler indexes public profiles based on `profileVisibility !== private`; there is no separate user consent for AI indexing. Adding consent changes privacy/product semantics and likely data shape. | CEO / CTO / Security | Decide default consent posture and wording, then add a minimal durable setting such as `allowAiIndexing` in privacy settings and enforce it in `crawlPublicProfiles`. | `server/src/services/aiContextIndex.ts`, `server/src/services/profileNormalization.ts`, `server/src/validation/requestSchemas.ts`, profile/privacy UI | 2026-05-27 |
| Stricter applicant/member API isolation | Auth/role hardening | requires CTO/security decision | Applicant frontend routing is restricted, but several member-authenticated backend routes accept any canonical identity. A stricter backend role guard is safer but changes API access behavior for applicant accounts. | CTO / Security / CEO | Approve applicant API isolation policy, then add member-only route guards while preserving applicant status/application routes. | `server/src/middleware.ts`, `server/src/routes/ai.ts`, `server/src/routes/social.ts`, `server/src/routes/courses.ts`, `server/src/routes/userCourses.ts`, `server/src/routes/reflection.ts`, applicant routes | 2026-05-27 |
